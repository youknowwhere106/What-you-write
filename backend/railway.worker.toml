[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.workers.celery_app worker --loglevel=info -Q ai_processing --concurrency=2"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
